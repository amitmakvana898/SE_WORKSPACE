#include <stdio.h>
#include <conio.h>

void main()
{
    int base = 10;
    int height = 21;
    int area = (base * height) / 2;

    printf("Area of triangle = %d", area);

    getch();
}